[project]
name = "mlx-dictate"
version = "0.1.0"
description = "Local push-to-talk dictation on Apple Silicon with MLX Whisper"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mlx-whisper>=0.4.3",
    "pynput>=1.8.2",
]
